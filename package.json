{
  "name": "ruleta-premios-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && firebase deploy",
    "deploy:channel": "vite build && firebase hosting:channel:deploy preview"
  },
  "dependencies": {
    "@types/xlsx": "^0.0.35",
    "popmotion": "^11.0.5",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.2.0",
    "vite": "^5.4.20",
    "vite-plugin-pwa": "^0.17.0"
  },
  "engines": {
    "node": ">=18"
  }
}
